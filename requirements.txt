# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
biopython>=1.81

# ML / clustering / dimensionality reduction
scikit-learn>=1.3
umap-learn>=0.5.4
hdbscan>=0.8.33

# Alignment / string distance
parasail>=1.3.4
python-Levenshtein>=0.21

# Plotting
plotly>=5.18
matplotlib>=3.7
seaborn>=0.13
logomaker>=0.8
kaleido>=0.2.1        # static PNG/PDF export for plotly

# Dashboard
streamlit>=1.32

# Excel export
openpyxl>=3.1
xlsxwriter>=3.1

# Optional: protein language model embeddings (large download, optional flag)
# torch>=2.1
# fair-esm>=2.0.0
# transformers>=4.38

# CDR/FR numbering (install via conda-forge/bioconda, NOT reliably via pip):
# conda install -c bioconda anarci
